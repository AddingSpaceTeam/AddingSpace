# Generated by tools/gen_tags.nim from ../doc/tags.md. DO NOT EDIT!

import tags

type
  RgcStmt* = enum
    NoStmt
    PassS = (ord(PassTagId), "pass")  ## pass declaration
    ModuleS = (ord(ModuleTagId), "module")  ## module declaration
    StmtsS = (ord(StmtsTagId), "stmts")  ## statement list
    InputS = (ord(InputTagId), "input")  ## pass input declaration
    OutputS = (ord(OutputTagId), "output")  ## pass output declaration
    UseS = (ord(UseTagId), "use")  ## module usage declaration
    ConnectS = (ord(ConnectTagId), "connect")  ## resource connection
    ExternalS = (ord(ExternalTagId), "external")  ## external resource declaration
    RasterS = (ord(RasterTagId), "raster")  ## raster pass body
    ComputeS = (ord(ComputeTagId), "compute")  ## compute pass body
    CopyS = (ord(CopyTagId), "copy")  ## copy pass body
    ShaderS = (ord(ShaderTagId), "shader")  ## shader binding
    UsageS = (ord(UsageTagId), "usage")  ## usage annotation

proc rawTagIsRgcStmt*(raw: TagEnum): bool {.inline.} =
  raw >= PassTagId and raw <= UsageTagId

type
  RgcType* = enum
    NoType
    ImageT = (ord(ImageTagId), "image")  ## image resource, T is generic param, actually mostly hides imageViews, maybe it also need separated type
    BufferT = (ord(BufferTagId), "buffer")  ## buffer resource of T

proc rawTagIsRgcType*(raw: TagEnum): bool {.inline.} =
  raw >= ImageTagId and raw <= BufferTagId

type
  RgcExpr* = enum
    NoExpr
    DotE = (ord(DotTagId), "dot")  ## owner.resource
    ColorE = (ord(ColorTagId), "color")  ## RGBA clear color value

proc rawTagIsRgcExpr*(raw: TagEnum): bool {.inline.} =
  raw >= DotTagId and raw <= ColorTagId

